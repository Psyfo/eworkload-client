query position($positionId: String!) {
    position(positionId: $positionId) {
        positionId
        name
        description
    }
}
query positions {
    positions {
        positionId
        name
        description
    }
}
