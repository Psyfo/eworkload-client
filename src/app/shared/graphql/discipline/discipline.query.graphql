query discipline($disciplineId: String!) {
    discipline(disciplineId: $disciplineId) {
        disciplineId
        name
        description
    }
}
query disciplines {
    disciplines {
        disciplineId
        name
        description
    }
}
