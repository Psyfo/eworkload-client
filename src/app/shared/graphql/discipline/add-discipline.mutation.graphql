mutation addDiscipline(
    $disciplineId: String!
    $name: String!
    $description: String!
) {
    addDiscipline(
        disciplineId: $disciplineId
        name: $name
        description: $description
    ) {
        disciplineId
        name
        description
    }
}
