query department($departmentId: String!) {
    department(departmentId: $departmentId) {
        departmentId
        name
        facultyId
        faculty {
            facultyId
            name
        }
    }
}
query departments {
    departments {
        departmentId
        name
        facultyId
        faculty {
            facultyId
            name
        }
    }
}
