query department($departmentId: String!) {
    department(departmentId: $departmentId) {
        departmentId
        name
        faculty {
            facultyId
            name
        }
    }
}
query departments {
    departments {
        departmentId
        name
        faculty {
            facultyId
            name
        }
    }
}
