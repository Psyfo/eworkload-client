query workFocus($name: String!) {
    workFocus(name: $name) {
        name
        teachingRatio
        researchRatio
        serviceRatio
    }
}
query workFocuses {
    workFocuses {
        name
        teachingRatio
        researchRatio
        serviceRatio
    }
}
