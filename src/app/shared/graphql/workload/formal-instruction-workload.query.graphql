query formalInstructionWorkload($userId: String) {
    formalInstructionWorkload(userId: $userId) {
        totalBaseHours
        totalOtherHours
        totalOverallHours
        percentageOfTotal
    }
}
