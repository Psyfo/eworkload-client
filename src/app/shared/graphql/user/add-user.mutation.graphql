mutation addUser($user: UserInput) {
    addUser(user: $user) {
        userId
        password
        email
        firstName
        lastName
        photoUrl
        disciplineId
        positionId
        gender
        workFocusName
        nationality
    }
}
