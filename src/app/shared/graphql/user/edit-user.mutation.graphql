mutation editUser($user: UserInput) {
    editUser(user: $user) {
        userId
        password
        email
        firstName
        lastName
        photoUrl
        disciplineId
        positionId
        workFocusName
        gender
        nationality
    }
}
