mutation deleteUser($user: UserInput) {
    deleteUser(user: $user) {
        userId
        password
        email
        firstName
        lastName
        photoUrl
        disciplineId
        positionId
        gender
        workFocusName
        nationality
    }
}
