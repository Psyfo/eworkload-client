mutation addBlock($blockId: String!, $name: String!, $description: String!) {
    addBlock(blockId: $blockId, name: $name, description: $description) {
        blockId
        name
        description
    }
}
