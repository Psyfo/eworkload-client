query block($blockId: String!) {
    block(blockId: $blockId) {
        blockId
        name
        description
    }
}
query blocks {
    blocks {
        blockId
        name
        description
    }
}
