query student($studentId: String!) {
    student(studentId: $studentId) {
        studentId
        email
        firstName
        lastName
        title
        year
    }
}
query students {
    students {
        studentId
        email
        firstName
        lastName
        title
        year
    }
}
